<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('label.date') %>:</strong>
  <%= @meal.date %>
</p>

<p>
  <strong><%= t('type') %>:</strong>
  <%= t( @meal.meal_type) %>
</p>

<p>
  <strong><%= t('order') %>:</strong><br/>
  <table>
    <thead>
      <tr>
        <th><%= t('name') %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @meal.orders.each do |order| %>
        <tr>
          <td><%= link_to order.dish.name, order.dish %></td>
          <td><%= link_to t('destroy'), order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>  
</p>



<%= link_to t('edit'), edit_meal_path(@meal) %> |
<%= link_to t('back'), meals_path %>
