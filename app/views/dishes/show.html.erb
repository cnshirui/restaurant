<p id="notice"><%= notice %></p>

<p>
  <strong><%= "#{t('name')}:" %></strong>
  <%= @dish.name %>
</p>

<p>
  <strong><%= "#{t('book')}:" %></strong>
  <%= link_to @dish.book.name, book_path(@dish.book) %>
</p>

<p>
  <strong><%= t('page') + ':' %></strong>
  <%= auto_link @dish.page, :html => { :target => '_blank' } %>
</p>

<p>
  <strong><%= t('material') %></strong>
  <table>
    <thead>
      <tr>
        <th><%= t('name') %></th>
        <th><%= t('quantity') %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @dish.burdens.each do |burden| %>
        <tr>
          <td><%= link_to burden.material.name, burden.material %></td>
          <td><%= burden.quantity %> g</td>
          <td><%= link_to t(t('destroy')), burden, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</p>

<%= link_to t('edit'), edit_dish_path(@dish) %> |
<%= link_to t('back'), dishes_path %>
