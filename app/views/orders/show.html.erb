<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('meal') %>:</strong>
  <%= link_to "#{@order.meal.date} - " + t( @order.meal.meal_type), meal_path(@order.meal) %>
</p>

<p>
  <strong>t('dish'):</strong>
  <%= link_to @order.dish.name, dish_path(@order.dish) %>
</p>

<%= link_to t('edit'), edit_order_path(@order) %> |
<%= link_to t('back'), orders_path %>
